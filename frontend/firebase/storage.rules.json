{
  "rules": {
    "storage": {
      "rules": {
        // Allow read access to all users
        "products/{productId}/{fileName}": {
          allow read: true;
          // Allow writes only for signed-in admin
          allow write: if 
            request.auth != null &&
            request.auth.token.email == 'mohamedtareq543219@gmail.com';
        }
      }
    }
  }
}